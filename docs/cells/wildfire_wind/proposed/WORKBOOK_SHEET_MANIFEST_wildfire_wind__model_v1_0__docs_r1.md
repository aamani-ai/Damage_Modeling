# Workbook manifest — wildfire_wind model v1.0/docs r1

Workbook: `damage_curve_records_wildfire_wind__model_v1_0__docs_r1.xlsx`

| Sheet | Purpose |
|---|---|
| `README` | Identity, grade, supported outputs, and guardrail |
| `Curves` | Exact two-unit class-state tables and Tier-4 labels |
| `KATs` | Fourteen numerical lookup fixtures |
| `Sources` | Load-bearing source and assumption register |
| `Claims` | Governed claims, inferences, and update triggers |
| `Tiers` | Parameter-by-parameter evidence grade |
| `Values` | Same-unit denominator, spatial grain, and scenario-loss withholding |

The JSON artifact and CSV registers are authoritative; the workbook is their review companion.
