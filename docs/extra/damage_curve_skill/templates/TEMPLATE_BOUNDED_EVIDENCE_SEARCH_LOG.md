# Bounded evidence search log — <cell_id>

## Claim boundary

```text
Within the documented surfaces, queries, endpoint tests, and cutoff, the review did not locate <precise evidence object>.
```

Do not turn this into a universal absence claim. State what private, unpublished, non-indexed, new, non-English, or differently described evidence may remain outside scope.

## Protocol

```yaml
review_cutoff:
language_scope:
access_scope:
target_hazard:
target_asset:
target_failure_units:
target_endpoint:
search_surfaces:
```

## Query families

| Family | Representative terms/variants | Endpoint sought | Result |
|---|---|---|---|
| | | | |

## Inclusion and exclusion

```text
required evidence chain:
source hazard -> local exposure -> matched failure/replacement state -> same-unit economic denominator

included source/endpoint types:
excluded source/endpoint types:
```

## Results and disposition

| Evidence link | Located evidence | Missing link | Governed action |
|---|---|---|---|
| | | | retain / withhold / re-source / defer |

## Reproducibility and update triggers

Record the linked source IDs, exact locators, search date, and the event that requires re-running the search. A negative-evidence claim cannot be load-bearing unless this log is linked from the claim register.
